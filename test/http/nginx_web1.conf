server {
    listen       80;
    server_name  localhost;

    location / {
        default_type text/plain;
        return 200 'i am web1\n';
    }

    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;
}
